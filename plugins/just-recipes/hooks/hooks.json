{
  "description": "Nudge raw Bash toward just recipes in every repo, from the project justfile or the global library (strict mode blocks)",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          { "type": "command", "command": "bash \"${CLAUDE_PLUGIN_ROOT}\"/hooks/enforce-just.sh", "timeout": 10 }
        ]
      }
    ]
  }
}
