---
name: example-phantom-cron
cron: # heartbeats-unit: example-phantom a b
command: /usr/local/bin/example-job --flag
log: /var/log/example/phantom-cron.log
enabled: true
---
Synthetic fixture: the second shape of the same defect. Five whitespace-
separated fields again, but the rendered line begins with UNIT_PREFIX, so
region_units would attribute it to a unit named `example-phantom` that has no
unit file anywhere. The leading `#` is what lets it reach that prefix, so the
one leading-`#` guard closes this shape too.
