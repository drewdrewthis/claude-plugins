<!-- Frontmatter mirrors templates/_frontmatter.partial.md (schema SSOT, itself mirroring specs/RECORD_FRONTMATTER.md) — edit there first, then here. -->
---
id: proc.<category>.<name>.evolution
kind: procedure
date: YYYY-MM-DD
keywords: [<named entities from the procedure's own keywords, plus: evolution>]
links: { procedures: [proc.<category>.<name>] }
status: active
description: <One line, ~120 chars: the throughline across this procedure's changes — what kept failing and what fixed it, not a topic label.>
---

# Evolution — <name>

## YYYY-MM-DD — <what changed, in five words>

<What the procedure used to say. What went wrong, with the evidence that proved it — a
number, a log line, a quote. What it says now, and the general lesson if there is one.>

<!--
HOW TO USE THIS TEMPLATE — delete this block.

EVOLUTION.md is where the WHY lives. The sibling PROCEDURE.md is present-tense only: when
you cut rationale, backstory, or a "this step is not holding" confession out of a procedure,
it lands here rather than being deleted.

Newest entry FIRST. Never rewrite an older entry — it was true when written.

An entry is worth writing when a future agent would otherwise re-make the mistake or
re-litigate the decision. "Fixed a typo" is not an entry; git log already has it.

Every entry carries its evidence. "It wasn't working" is not an entry. "192 lines, last
checkpoint 8 days stale" is.
-->
