<!-- TEMPLATE — every procedure directory carries an EVOLUTION.md next to its
     PROCEDURE.md. One dated line per material change, newest first: what
     changed and WHY (the incident/correction that motivated it). This is the
     procedure's memory — without it, the next agent re-litigates settled
     edits. Append on every /log or /evolve touch; never rewrite history. -->
# <procedure name> evolution log

- <YYYY-MM-DD> — <what changed>: <the incident or correction that motivated it>
